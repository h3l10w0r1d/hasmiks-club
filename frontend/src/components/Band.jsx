import Reveal from './Reveal'

// The rose-tinted divider bar. Text intentionally removed per client request —
// the colored band stays as a visual separator between the hero and the
// sections below. (Band copy still lives in data/content.js if it's ever
// wanted back.)
export default function Band() {
  return <Reveal as="div" className="band" />
}
